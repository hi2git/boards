version: '3.4'

services:
  boards.front:
    environment:
      - ASPNETCORE_ENVIRONMENT=Docker
    ports:
      - "8080:80"
    volumes:
      - D:/Boards:/usr/share/boards
  #boards.posts:
  #  environment:
  #      - ASPNETCORE_ENVIRONMENT=Docker
  #  ports:
  #      - "8081:80"
  boards.posts:
    environment:
      - ASPNETCORE_ENVIRONMENT=Docker
      - ASPNETCORE_URLS=http://+:80
    ports:
      - "8081:80"
    volumes:
      - D:/Boards:/usr/share/boards
